// join strings
["a","b"].concat(["c","d"]);
// split strings
"Hello world!".split(" ");

// get a list of attributes
list_of_attributes = Object.keys(js_object);

// slicing array
[4,5,6].slice(1,2);

// global variables
function() {
  var window.global_variable = 34;
}()

// parse Float
parseFloat("0.4")
